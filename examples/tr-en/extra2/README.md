# examples/tr-en/extra2/

onde テキストを対象とした追加言語の翻訳・評価を行います。
新規翻訳モデル（`gpt-oss:120b`）の翻訳能力と、異なる実行環境（プロバイダー）間での挙動の差異を検証するためのセットです。

対象言語: エスペラント (eo), ヒンディー語 (hi), テルグ語 (te), カンナダ語 (kn), トルコ語 (tr), エストニア語 (et), セルビア語 (sr), 朝鮮語 (ko)

## 実行

`make` で翻訳・評価・集計を一括実行。ローカル（Ollama）とクラウドAPI（OpenRouter）の2つの完全に独立したパイプラインで処理を行います。

- 翻訳モデル: `gpt-oss:120b` (Ollama および OpenRouter)
- 評価モデル: `qwen3.6` (Ollama および OpenRouter)
- 設定: threshold=20・keep=5・CoT なし・用語ファイル注入（`../../terms/*-en.{json,tsv}`）

**出力先**:
- ローカル (Ollama): 翻訳 `tr/`、評価 `evals/`、スコア `SCORES.txt`
- クラウド (OpenRouter): 翻訳 `tr-openrouter/`、評価 `evals-openrouter/`、スコア `SCORES-openrouter.txt`

**注意点**: `gpt-oss:120b` は CoT（思考プロセス）の出力を無効にできないため、翻訳に非常に時間がかかります。本ディレクトリでの翻訳実行は、あくまで品質検証とプロバイダー比較を目的としています。

## 翻訳品質の概要 (gpt-oss:120b)

評価結果（`SCORES.txt` / ローカルOllama版）および内容の検証に基づく各言語の品質傾向は以下の通りです。`gpt-oss:120b` によるマイナー言語の翻訳は、環境（Ollama/OpenRouter）を問わず極めて不安定で、多くの言語で実用に耐えない結果となりました。

**実用レベル (70点以上):** トルコ語 (tr)

**中〜低品質 (50点〜70点未満):** セルビア語 (sr), エスペラント (eo), エストニア語 (et), 朝鮮語 (ko)

**致命的な欠陥 (50点未満):** カンナダ語 (kn), ヒンディー語 (hi), テルグ語 (te)

全体を通して、モデルの規模に反して非常に脆く、以下のような致命的な幻覚（ハルシネーション）が頻発しました。

- **多言語の混入**: カンナダ語の翻訳中に突然「朝鮮語」が混ざったり、テルグ語に「アラビア語」が混入するなど、言語の境界が崩壊する事象が確認されました。
- **システムプロンプトの漏洩**: セルビア語やエスペラント語などで、翻訳の途中に突然「The conversation ended.」や「Need final translation:」といったメタな指示文（プロンプトの残骸）がそのまま出力されるバグが発生しました。
- **フォーマットの喪失**: 特に対話スクリプトに必須である話者タグ（Camille: や Luc:）を頻繁に出力し忘れるという構造上の欠陥が見られました。（朝鮮語などで顕著に発生）

## 評価環境（プロバイダー）の違いによる挙動の差異

### 1. エラー検知能力の完全な一致
同一の評価モデル（`qwen3.6`）を用いた場合、**ローカル（Ollama）でもクラウド（OpenRouter）でも、テキストに含まれる「文字化け」「他言語の混入」「プロンプトの漏洩」といったエラーを寸分違わず正確に検知・指摘する能力（読む力）を持っている**ことが証明されました。スコアに生じる数点〜10点程度のブレはプロバイダーの能力差ではなく、推論（サンプリング）ごとのランダムな揺らぎに過ぎません。

### 2. 構造化出力（JSONフォーマット）の安定性の違い
運用上の重要な知見として、構造化出力のバグの出方にプロバイダー間で明確な差が見られました。

- **OpenRouter（クラウド）**: 構造化出力が不安定になることがあり、`reasoning` フィールドに「無意味な内容」が入って出力が破損するケースが複数回発生しました。この場合、JSONの構文としては成立してしまうため、プログラムによるエラー検知が難しく、目視確認と手動での再実行が必要でした。
- **Ollama（ローカル）**: 無意味な内容が出力される破損は発生しませんでしたが、代わりに `overall_comment` フィールドの出力が「無限ループ」に陥る事象が一定割合で発生しました。ただし、こちらはツール側のループ検知機能によって自動的にリトライが行われるため、運用上の手間は抑えられています。

完全な自動化パイプラインを構築する上では、プロバイダーごとのこうした「ハルシネーションの出方のクセ」を考慮したエラーハンドリングが必要不可欠であることが示唆されています。